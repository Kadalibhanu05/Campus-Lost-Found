<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Campus Lost & Found</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: #f8fafc; }
        [x-cloak] { display: none !important; }
    </style>
</head>
<body class="flex flex-col min-h-screen text-slate-800 bg-slate-50">

    <header class="bg-indigo-900 text-white shadow-lg sticky top-0 z-50" x-data="{ mobileMenuOpen: false, userDropdownOpen: false }">
        <nav class="container mx-auto px-4 sm:px-6 lg:px-8 h-20 flex justify-between items-center">
            
            <a href="/" class="flex items-center gap-2 group">
                <div class="w-10 h-10 bg-amber-400 rounded-xl flex items-center justify-center text-indigo-900 font-extrabold text-xl shadow-lg transform group-hover:rotate-6 transition-transform">
                    L
                </div>
                <div class="flex flex-col -space-y-1">
                    <span class="text-lg font-bold tracking-tight text-white">CAMPUS</span>
                    <span class="text-xs font-bold tracking-widest text-amber-400 uppercase">Lost & Found</span>
                </div>
            </a>

            <div class="hidden md:flex items-center space-x-6">
                
                <a href="/report" class="flex items-center gap-2 px-5 py-2.5 bg-amber-500 hover:bg-amber-400 text-indigo-900 font-bold rounded-full transition-all shadow-md transform hover:-translate-y-0.5">
                    <span>+</span> Report Item
                </a>

                <% if (user) { %>
                    <a href="/my-posts" class="text-sm font-bold text-indigo-200 hover:text-white transition-colors duration-200">
                        My Posts
                    </a>

                    <div class="relative">
                        <button @click="userDropdownOpen = !userDropdownOpen" @click.away="userDropdownOpen = false" class="flex items-center gap-3 focus:outline-none group">
                            <div class="text-right hidden lg:block">
                                <p class="text-sm font-semibold text-white group-hover:text-amber-300 transition"><%= user.name %></p>
                            </div>
                            <div class="h-10 w-10 rounded-full bg-indigo-700 border-2 border-amber-400 text-amber-400 flex items-center justify-center font-bold text-lg shadow-sm">
                                <%= user.name.charAt(0).toUpperCase() %>
                            </div>
                        </button>

                        <div x-show="userDropdownOpen" x-transition.origin.top.right x-cloak
                             class="absolute right-0 mt-4 w-56 bg-white rounded-2xl shadow-xl py-2 border border-slate-100 ring-1 ring-black ring-opacity-5 text-slate-700 z-50">
                            <div class="px-4 py-3 border-b border-slate-100">
                                <p class="text-sm font-medium text-slate-500">Signed in as</p>
                                <p class="text-sm font-bold text-indigo-600 truncate"><%= user.email %></p>
                            </div>
                            <a href="/my-posts" class="block px-4 py-3 text-sm font-medium hover:bg-indigo-50 hover:text-indigo-700 transition">
                                📦 My Posts
                            </a>
                            <div class="border-t border-slate-100 my-1"></div>
                            <a href="/logout" class="block px-4 py-3 text-sm font-medium text-red-600 hover:bg-red-50 transition">
                                Log Out
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    <div class="flex items-center gap-4">
                        <a href="/login" class="text-sm font-semibold text-indigo-100 hover:text-white transition">Log In</a>
                        <a href="/signup" class="px-5 py-2 bg-indigo-700 hover:bg-indigo-600 text-white text-sm font-semibold rounded-lg shadow-md transition">
                            Sign Up
                        </a>
                    </div>
                <% } %>
            </div>

            <div class="md:hidden flex items-center">
                <button @click="mobileMenuOpen = !mobileMenuOpen" class="text-indigo-100 hover:text-white focus:outline-none">
                    <svg class="h-7 w-7" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path x-show="!mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        <path x-show="mobileMenuOpen" x-cloak stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>
        </nav>

        <div x-show="mobileMenuOpen" x-collapse x-cloak class="md:hidden bg-indigo-800 border-t border-indigo-700">
            <div class="px-4 py-6 space-y-4">
                <a href="/report" class="block w-full text-center px-4 py-3 bg-amber-500 text-indigo-900 font-bold rounded-lg shadow-md">
                    + Report Item
                </a>
                
                <% if (user) { %>
                    <div class="pt-4 border-t border-indigo-700">
                        <div class="flex items-center gap-3 mb-4 px-2">
                            <div class="h-10 w-10 rounded-full bg-indigo-600 border border-amber-400 text-amber-400 flex items-center justify-center font-bold">
                                <%= user.name.charAt(0).toUpperCase() %>
                            </div>
                            <div>
                                <p class="text-sm font-bold text-white"><%= user.name %></p>
                                <p class="text-xs text-indigo-300">Logged In</p>
                            </div>
                        </div>
                        <a href="/my-posts" class="block px-4 py-2 text-indigo-100 hover:bg-indigo-700 rounded-lg">My Posts</a>
                        <a href="/logout" class="block px-4 py-2 text-red-300 hover:bg-indigo-700 rounded-lg">Log Out</a>
                    </div>
                <% } else { %>
                    <div class="grid grid-cols-2 gap-3 mt-4">
                        <a href="/login" class="text-center px-4 py-2 bg-indigo-700 rounded-lg font-semibold text-white">Log In</a>
                        <a href="/signup" class="text-center px-4 py-2 bg-white text-indigo-900 rounded-lg font-bold">Sign Up</a>
                    </div>
                <% } %>
            </div>
        </div>
    </header>
    <main class="flex-grow">